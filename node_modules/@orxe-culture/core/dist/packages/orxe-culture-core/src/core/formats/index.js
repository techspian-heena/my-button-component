//  TODO: Add localization support,
// Added Decimal, Currency, and  string format support
import { i18nDefault } from '..';
import { currency, decimal, NumberFormatPartTypes } from './number-format';
import { capitalize, lowerCase, upperCase, StringFormat } from './string-format';
export function defaultInterpolationFormat(value, format, lang = i18nDefault.language) {
    //  TODO: add Localize
    if (!value)
        return value;
    switch (format) {
        case NumberFormatPartTypes.CURRENCY:
            return currency(value, lang);
        case NumberFormatPartTypes.DECIMAL:
            return decimal(value, lang);
        case StringFormat.UPPER_CASE:
            return upperCase(value);
        case StringFormat.LOWER_CASE:
            return lowerCase(value);
        case StringFormat.CAPITALIZE:
            return capitalize(value);
        default:
            return value;
    }
}
//# sourceMappingURL=index.js.map